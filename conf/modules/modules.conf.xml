<configuration name="modules.conf" description="Modules">
  <modules>

    <!--
      Config for all but mod_event_socket are loaded in order.
      Config for mod_event_socket is loaded last, but must be present.
      Config files acl.conf.xml and post_load_switch.conf.xml
      are loaded just before event socket config.
    -->

    <!-- Loggers -->
    <load module="mod_console"/>
    <load module="mod_logfile"/>

    <!-- Event Handlers. This is -->
    <load module="mod_event_socket"/>

    <!-- Endpoints -->
    <load module="mod_loopback"/>

    <!-- Applications -->
    <load module="mod_commands"/>
    <load module="mod_conference"/>
    <load module="mod_curl"/>
    <load module="mod_dptools"/>
    <load module="mod_expr"/>
    <load module="mod_hash"/>
    <load module="mod_voicemail"/>

    <!-- Dialplan Interfaces -->
    <load module="mod_dialplan_xml"/>

    <!-- Codec Interfaces -->
    <load module="mod_h26x"/>
    <load module="mod_opus"/>

    <!-- File Format Interfaces -->
    <load module="mod_av"/>
    <load module="mod_sndfile"/>
    <load module="mod_native_file"/>
    <load module="mod_png"/>
    <load module="mod_local_stream"/>
    <load module="mod_tone_stream"/>

    <!-- Languages -->
    <load module="mod_lua"/>

    <!-- Say -->
    <load module="mod_say_en"/>

  </modules>
</configuration>
