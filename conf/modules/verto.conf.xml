<configuration name="verto.conf" description="HTML5 Verto Endpoint">

  <settings>
    <param name="debug" value="$${chat_debug_level}"/>
    <!-- <param name="enable-presence" value="true"/> -->
    <!-- <param name="enable-fs-events" value="true"/> -->
    <!-- <param name="detach-timeout-sec" value="120"/> -->
  </settings>

  <profiles>
    <profile name="chat4">
      <param name="secure-combined" value="$${certs_dir}/wss.pem"/>
      <param name="bind-local" value="127.0.0.1:$${chat_ws_port}"/>
      <param name="force-register-domain" value="chat"/>
      <param name="userauth" value="true"/>
      <param name="blind-reg" value="false"/>
    </profile>
    <profile name="chat6">
      <param name="secure-combined" value="$${certs_dir}/wss.pem"/>
      <param name="bind-local" value="[::1]:$${chat_ws_port}"/>
      <param name="force-register-domain" value="chat"/>
      <param name="userauth" value="true"/>
      <param name="blind-reg" value="false"/>
    </profile>
  </profiles>

</configuration>
